kind = "web"
previewPath = "/"
title = "Election Integrity Dashboard"
version = "1.0.0"
id = "artifacts/election-dashboard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20761

[services.development]
run = "pnpm --filter @workspace/election-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/election-dashboard", "run", "build" ]
publicDir = "artifacts/election-dashboard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20761"
BASE_PATH = "/"
